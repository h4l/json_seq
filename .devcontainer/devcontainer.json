{
  "dockerFile": "Dockerfile",
  "extensions": ["denoland.vscode-deno", "editorconfig.editorconfig"],
  "build": {
    "args": { "DENO_VERSION": "1.18.2" }
  },

  // Comment out connect as root instead. More info: https://aka.ms/vscode-remote/containers/non-root.
  "remoteUser": "node"
}
